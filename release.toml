[workspace]
allow-branch = ["main"]
# Per-crate tags: psh-bar-v0.2.0, psh-core-v0.1.9, etc.
tag-name = "{{crate_name}}-v{{version}}"
tag-message = "{{crate_name}} v{{version}}"
# Only release crates that actually changed
independent-releases = true
