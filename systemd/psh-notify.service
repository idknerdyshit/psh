[Unit]
Description=psh notification daemon
PartOf=psh.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/psh-notify
Restart=on-failure
RestartSec=2

[Install]
WantedBy=psh.target
