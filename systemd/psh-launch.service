[Unit]
Description=psh application launcher
PartOf=psh.target
After=graphical-session.target psh-bar.service

[Service]
Type=simple
ExecStart=/usr/bin/psh-launch
Restart=on-failure
RestartSec=2

[Install]
WantedBy=psh.target
